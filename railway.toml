[build]
builder = "NIXPACKS"
buildCommand = "cd server && go build -o server ."

[deploy]
startCommand = "cd server && ./server"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
